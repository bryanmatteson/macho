# macho-symbols

Mach-O symbol-table helpers and presentation adapters.

Dyld import/export traversal lives in `macho-dyld`. Process-free language
demangling lives in `macho-demangle` and is reexported here for symbol-oriented
callers.
