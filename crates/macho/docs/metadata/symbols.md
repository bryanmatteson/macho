# macho::metadata::symbols

Mach-O symbol-table helpers and presentation adapters.

Dyld import/export traversal lives in `macho::metadata::dyld`. Process-free language
demangling lives in `macho::metadata::demangle` and is reexported here for symbol-oriented
callers.
